import React from 'react';
import { Text } from 'react-native';

const UserName = () => {
  const UserName = 'Isabella de Paula'; 

  return (
      <Text style={{ color: '#fff', fontSize: 20, marginTop: 11 }}>{UserName}</Text>
  );
};

export default UserName;
